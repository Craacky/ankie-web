[
  {
    "question": "Что такое Docker?",
    "answer": "Docker — это платформа с открытым исходным кодом, которая автоматизирует развертывание, масштабирование и управление приложениями с помощью контейнеризации. Docker позволяет упаковывать приложения и их зависимости в легковесные и переносимые контейнеры, которые могут работать в любой системе с поддержкой Docker.\n\n**Преимущества:**\n- Изоляция приложений и зависимостей\n- Переносимость между различными средами\n- Быстрое развертывание и масштабирование\n- Устойчивость к конфликту зависимостей\n\n---"
  },
  {
    "question": "Опишите составные части архитектуры Docker",
    "answer": "Архитектура Docker включает следующие компоненты:\n\n- **Docker Client** — интерфейс командной строки (CLI), с помощью которого пользователи управляют Docker-демоном\n- **Docker Host** — хост, на котором запущен Docker-демон, а также контейнеры, образы, сети и тома\n- **Docker Daemon** — фоновый процесс, который слушает Docker API-запросы и управляет Docker-объектами (образами, контейнерами, сетями, томами и т.д.)\n- **Docker Registry** — репозиторий для хранения и распространения Docker-образов (например, Docker Hub)\n- **Docker Images** — неизменяемые шаблоны для создания контейнеров\n- **Docker Containers** — запущенные экземпляры Docker-образов\n\n---"
  },
  {
    "question": "Какие примеры использования Docker?",
    "answer": "Примеры использования Docker:\n\n- **Разработка и тестирование** — обеспечение одинаковой среды разработки для всех участников команды\n- **Непрерывная интеграция и развертывание (CI/CD)** — автоматизация процесса сборки и развертывания приложений\n- **Микросервисы** — разбиение приложения на небольшие независимые сервисы, которые могут быть развернуты и масштабированы отдельно\n- **Облачные вычисления** — использование контейнеров для эффективного размещения приложений в облаке\n- **Кроссплатформенность** — возможность запуска одного и того же приложения на разных операционных системах\n\n---"
  },
  {
    "question": "Как определить состояние контейнера Docker?",
    "answer": "Для определения состояния контейнера Docker можно использовать команду:\n```bash\ndocker ps -a\n```\n\nЭта команда показывает все контейнеры и их текущее состояние (running, exited, paused и т.д.).\n\nТакже можно использовать:\n```bash\ndocker inspect <container_id>\n```\nдля получения подробной информации о контейнере, включая его состояние, конфигурацию, метаданные и т.д.\n\n---"
  },
  {
    "question": "Расскажите о наиболее общих командах Docker?",
    "answer": "- `docker run` — создает и запускает новый контейнер\n- `docker ps` — показывает список запущенных контейнеров\n- `docker ps -a` — показывает все контейнеры (запущенные и остановленные)\n- `docker stop` — останавливает запущенный контейнер\n- `docker start` — запускает остановленный контейнер\n- `docker rm` — удаляет контейнер\n- `docker images` — показывает список всех образов\n- `docker rmi` — удаляет образ\n- `docker logs` — выводит логи контейнера\n- `docker exec` — запускает команду в работающем контейнере\n- `docker build` — собирает образ из Dockerfile\n- `docker pull` — скачивает образ из репозитория\n- `docker push` — загружает образ в репозиторий\n\n---"
  },
  {
    "question": "Расскажите о продвинутых командах Docker?",
    "answer": "- `docker stats` — отображает статистику использования ресурсов (CPU, память и т.д.) для контейнеров\n- `docker top` — показывает запущенные процессы в контейнере\n- `docker diff` — показывает изменения файловой системы контейнера\n- `docker port` — показывает сопоставление портов контейнера\n- `docker wait` — блокирует выполнение до завершения работы контейнера\n- `docker attach` — подключается к запущенному контейнеру\n- `docker commit` — создает образ из измененного контейнера\n- `docker export` — экспортирует файловую систему контейнера\n- `docker network create` — создает новую сеть\n- `docker volume create` — создает новый том\n\n---"
  },
  {
    "question": "Управление Docker",
    "answer": "Управление Docker включает:\n\n- Управление контейнерами (запуск, остановка, удаление)\n- Управление образами (сборка, тегирование, удаление)\n- Управление сетями (создание, подключение, удаление)\n- Управление томами (создание, монтирование, удаление)\n- Мониторинг и логирование (просмотр статистики, логов)\n- Безопасность (ограничение доступа, сканирование образов)\n- Оркестрация (использование Docker Compose, Swarm или Kubernetes)\n\n---"
  },
  {
    "question": "Что такое виртуализация?",
    "answer": "Виртуализация — это технология, которая позволяет создавать виртуальные версии физических ресурсов, таких как серверы, операционные системы, хранилище данных и сетевые ресурсы. В контексте вычислительных систем виртуализация позволяет запускать несколько виртуальных машин (VM) на одном физическом сервере с помощью гипервизора, который управляет распределением ресурсов между этими VM.\n\n---"
  },
  {
    "question": "Что такое гипервизор?",
    "answer": "Гипервизор — это программный слой, который создает и управляет виртуальными машинами (VM). Он позволяет нескольким операционным системам работать на одном физическом хосте, изолируя их друг от друга. Существуют два типа гипервизоров:\n\n- **Type 1 (голый металл)** — работает непосредственно на физическом оборудовании (например, VMware vSphere, Microsoft Hyper-V, Xen)\n- **Type 2** — работает поверх уже установленной операционной системы (например, Oracle VirtualBox, VMware Workstation)\n\n---"
  },
  {
    "question": "Различия контейнеризации и виртуализации?",
    "answer": "| Характеристика | Виртуализация | Контейнеризация |\n|----------------|---------------|------------------|\n| **Изоляция** | Полная изоляция с отдельной ОС | Процесс-уровневая изоляция в ядре |\n| **Производительность** | Накладные расходы из-за эмуляции ОС | Высокая производительность |\n| **Размер** | Больше места из-за ОС | Компактные образы |\n| **Запуск** | Медленнее из-за загрузки ОС | Быстрее |\n| **Портабельность** | Менее портативные | Высокая портабельность |\n| **Безопасность** | Лучшая изоляция | Меньше изоляции, но более гибкое управление |\n\n---"
  },
  {
    "question": "Что такое Docker контейнер?",
    "answer": "Docker контейнер — это легковесный, портативный и самодостаточный исполняемый пакет, который включает в себя приложение и все его зависимости (библиотеки, системные инструменты, параметры и т.д.). Контейнер — это запущенный экземпляр Docker-образа, который может быть создан, запущен, остановлен, перемещен или удалён. Контейнеры изолированы друг от друга и от хост-системы.\n\n---"
  },
  {
    "question": "Как реализовать автоматический перезапуск Docker контейнера?",
    "answer": "Для автоматического перезапуска контейнера используется флаг `--restart` с командой `docker run`:\n\n```bash\ndocker run --restart=always <image_name>\n```\n\nВарианты политики перезапуска:\n- `no` — не перезапускать контейнер (по умолчанию)\n- `on-failure[:max-retries]` — перезапускать только при сбоях с необязательным ограничением попыток\n- `unless-stopped` — перезапускать при сбоях, но не при ручной остановке\n- `always` — всегда перезапускать, независимо от причины остановки\n\n---"
  },
  {
    "question": "Жизненный цикл контейнера",
    "answer": "Жизненный цикл Docker-контейнера включает следующие этапы:\n\n1. **Создание** — контейнер создается из образа\n2. **Запуск** — контейнер запускается и начинает выполнение процесса\n3. **Работа** — контейнер выполняет задачи\n4. **Пауза** — контейнер может быть приостановлен (не все процессы могут быть приостановлены)\n5. **Остановка** — контейнер останавливается, процесс завершается\n6. **Удаление** — контейнер может быть удален\n\n---"
  },
  {
    "question": "Опишите все возможные состояния контейнера Docker",
    "answer": "- **Created** — контейнер был создан, но еще не запущен\n- **Running** — контейнер запущен и работает\n- **Paused** — контейнер приостановлен (все процессы в контейнере приостановлены)\n- **Restarting** — контейнер находится в процессе перезапуска\n- **Removing** — контейнер находится в процессе удаления\n- **Exited** — контейнер остановлен после завершения работы\n- **Dead** — контейнер не может быть остановлен или удалён\n\n---"
  },
  {
    "question": "Что такое Docker Compose?",
    "answer": "Docker Compose — это инструмент для определения и запуска многоконтейнерных Docker-приложений. С помощью Compose вы можете использовать YAML-файл для настройки сервисов вашего приложения, а затем с одной командой создавать и запускать все сервисы, определенные в конфигурации.\n\nПример команды:\n```bash\ndocker-compose up\n```\n\n**Преимущества:**\n- Определение всех сервисов в одном файле\n- Единая команда для запуска всех сервисов\n- Автоматическая настройка сетей между контейнерами\n\n---"
  },
  {
    "question": "Что такое Dockerfile?",
    "answer": "Dockerfile — это текстовый файл, содержащий последовательность инструкций для создания Docker-образа. Каждая инструкция в Dockerfile создает новый слой в образе. Docker читает инструкции из Dockerfile и выполняет их последовательно для автоматического создания образа.\n\nПример простого Dockerfile:\n```dockerfile\nFROM ubuntu:latest\nRUN apt-get update\nCOPY . /app\nWORKDIR /app\nCMD [\"python\", \"app.py\"]\n```\n\n---"
  },
  {
    "question": "В чем разница между CMD и ENTRYPOINT в Dockerfile?",
    "answer": "- **CMD** — предоставляет значения по умолчанию для запуска контейнера. Эти значения могут быть переопределены при запуске контейнера с помощью команды `docker run`.\n\n- **ENTRYPOINT** — конфигурирует контейнер для запуска как исполняемый файл. При использовании ENTRYPOINT команды, переданные в `docker run`, будут добавлены как аргументы к команде ENTRYPOINT.\n\n**Пример:**\n```dockerfile\nENTRYPOINT [\"python\"]\nCMD [\"app.py\"]\n```\nЗапуск: `docker run myimage test.py` выполнит `python test.py`.\n\n---"
  },
  {
    "question": "Что такое образ Docker?",
    "answer": "Docker-образ — это неизменяемый, исполняемый пакет, который включает в себя все необходимое для запуска приложения: код, библиотеки, среду выполнения, параметры и системные библиотеки. Образы используются для создания контейнеров. Образ Docker состоит из ряда слоев, каждый из которых представляет собой изменение в предыдущем слое.\n\n---"
  },
  {
    "question": "Что такое Docker Registry?",
    "answer": "Docker Registry — это сервис для хранения и распространения Docker-образов. Docker Hub — это публичный реестр, но организации могут использовать частные реестры. Registry позволяет загружать (push) и скачивать (pull) Docker-образы.\n\n---"
  },
  {
    "question": "Стандартные сети Docker?",
    "answer": "Docker создает три стандартных сети:\n\n- **Bridge** (по умолчанию) — сеть по умолчанию, используемая при запуске контейнера без указания другой сети\n- **Host** — убирает изоляцию сети между контейнером и хостом\n- **None** — отключает все сети для контейнера\n\nКоманда для просмотра сетей:\n```bash\ndocker network ls\n```\n\n---"
  },
  {
    "question": "Средства оркестрации",
    "answer": "Средства оркестрации Docker:\n\n- **Docker Swarm** — встроенный инструмент оркестрации Docker для управления кластером Docker-хостов\n- **Kubernetes** — популярная платформа для автоматизации развертывания, масштабирования и управления контейнеризированными приложениями\n- **Apache Mesos** — альтернативная платформа оркестрации\n- **Nomad** — простая и гибкая система оркестрации от HashiCorp\n\n---"
  },
  {
    "question": "Что такое Docker Swarm",
    "answer": "Docker Swarm — это встроенный в Docker инструмент оркестрации, который позволяет создавать и управлять кластером Docker-хостов. Swarm включает в себя распределенную систему управления контейнерами, автоматическое балансирование нагрузки, отказоустойчивость и масштабирование.\n\n**Основные компоненты:**\n- **Manager nodes** — управляют кластером и оркестрацией\n- **Worker nodes** — выполняют контейнеры\n- **Services** — определяют задачи, которые должны выполняться в кластере\n\n---"
  },
  {
    "question": "Что такое volumes в Docker?",
    "answer": "Volumes (тома) в Docker — это механизм для сохранения данных, создаваемых и используемых Docker-контейнерами. Volumes позволяют сохранять данные независимо от жизненного цикла контейнера. В отличие от связывания каталогов (bind mounts), volumes управляются Docker и обеспечивают лучшую производительность и безопасность.\n\nТипы:\n- **Named volumes** — именованные тома, создаваемые Docker\n- **Anonymous volumes** — неименованные тома\n- **Bind mounts** — монтирование хост-директории или файла\n\n---"
  },
  {
    "question": "Сохраняются ли данные при остановке контейнера?",
    "answer": "Нет, данные в файловой системе контейнера не сохраняются при его остановке. Когда контейнер удаляется, все его внутренние изменения теряются. Для сохранения данных необходимо использовать **тома (volumes)** или **связывание каталогов (bind mounts)**, которые позволяют хранить данные вне контейнера.\n\nПример использования тома:\n```bash\ndocker run -v myvolume:/data myimage\n```"
  }
]